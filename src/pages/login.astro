---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const pageTitle = 'Mitgliederbereich Login – Repair Café Leonberg';
const pageDescription = 'Anmeldung für den internen Mitgliederbereich des Repair Café Leonberg.';
export const prerender = false;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <Header />

  <main class="bg-slate-100 py-12 md:py-16">
    <section>
      <div class="container">
        <div class="mx-auto max-w-3xl text-center">
          <span class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-1 text-xs font-semibold uppercase tracking-[0.2em] text-slate-500 shadow-sm">
            Mitgliederbereich
          </span>
          <h1 class="mt-5 heading-hero">
            Anmeldung für Vereinsmitglieder
          </h1>
          <p class="mt-3 text-base text-slate-600">
            Der Zugang ist jetzt per Browser-Anmeldung (Basic Auth) geschützt. Ihr Browser fragt Sie automatisch nach Benutzername und Passwort – die Daten erhalten Sie vom Orga-Team.
          </p>
        </div>
      </div>
    </section>

    <section class="mt-12">
      <div class="container">
        <div class="grid gap-6">
          <div class="mx-auto w-full max-w-xl rounded-3xl bg-white p-8 text-center shadow-xl shadow-slate-900/5">
            <h2 class="heading-sub">So funktioniert der Zugang</h2>
            <p class="mt-3 text-sm text-slate-600">
              Öffnen Sie <strong>/members</strong> oder diese Login-Seite. Der Browser blendet ein kleines Dialogfenster für Benutzername und Passwort ein. Nach erfolgreicher Eingabe wird der Mitgliederbereich angezeigt.
            </p>
            <p class="mt-4 text-sm text-slate-600">
              Falls die Abfrage nicht erscheint, laden Sie die Seite neu oder probieren Sie einen anderen Browser. Zugangsdaten gibt es ausschließlich über das Orga-Team.
            </p>
            <div class="mt-6 text-sm text-slate-600">
              <a class="text-brand-700 hover:underline" href="mailto:info@repair-leonberg.de">info@repair-leonberg.de</a>
              &nbsp;|&nbsp;
              Info-Telefon: <a class="text-brand-700 hover:underline" href="tel:071529904977">07152 990 4977</a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>
